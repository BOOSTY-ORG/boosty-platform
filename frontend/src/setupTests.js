/**
 * Jest setup file
 */
import '@testing-library/jest-dom';

// Since we're running in node environment, we don't need browser-specific mocks
// for this simple test file that only exports functions

console.log('Jest setup completed');